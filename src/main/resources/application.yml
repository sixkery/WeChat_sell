# 开发环境配置
server:
  port: 8081


spring:
  #数据库的配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/sell_weixin?useUnicode=true&characterEncoding=utf-8
    username: root
    password: sixkery

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  application:
    name: sell

## 日志的配置
#logging:
#    # 日志文件存放位置
#  file:
#    name: log/blog-dev.log
## 日志的配置

